<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title class="titulo">Editar Película</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <%- include('./partials/header.ejs') %>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
    </figure>
    <h1 class="titulo">Película a Editar: <%= Movie.title %>
    </h1>
    <section class="formulario">
        <form action="/admin/update/<%= Movie.id %>?_method=PUT" method="POST">
            <div>
                <label for="title">Title </label>
                <p><input type="text" name="title" id="title" value="<%= Movie.title %>"></p>
                <small>
                    <%= locals.errores && errores.title ? errores.title.msg : null %>
                </small>
            </div>

            <div>
                <label for="rating">Rating </label>
                <p><input type="number" name="rating" id="rating" value="<%= Movie.rating %>"></p>
                <small>
                    <%= locals.errores && errores.rating ? errores.rating.msg : null %>
                </small>
            </div>

            <div>
                <label for="awards">Awards </label>
                <p><input type="number" name="awards" id="awards" value="<%= Movie.awards %>"></p>
                <small>
                    <%= locals.errores && errores.awards ? errores.awards.msg : null %>
                </small>
            </div>

            <div>
                <label for="release_date">Release date </label>
                <p><input type="date" name="release_date" id="release_date" value="<%= release_date %>">
                </p>
                <small>
                    <%= locals.errores && errores.release_date ? errores.release_date.msg : null %>
                </small>
            </div>

            <div>
                <label for="length">Length </label>
                <p><input type="number" name="length" id="" value="<%= Movie.length %>"></p>
                <small>
                    <%= locals.errores && errores.length ? errores.length.msg : null %>
                </small>
            </div>
            <br>

            <p> <button class="botonAgregar" type="submit">Actualizar</button>

                <a class="botonVolver" href="/movies">Listado de Películas</a>

                <a class="botonVolver" href="/admin/destroy/<%= Movie.id %>">¿Deseas borrar esta película?</a>
            </p>
        </form>
    </section>
</body>

</html>