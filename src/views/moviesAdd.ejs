<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movie add</title>
    <link rel='stylesheet' href='/css/style.css' />
</head>

<body>
    <%- include('./partials/header.ejs') %>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
      </figure>
    <h1 class="titulo">Agregar Película</h1>
    <br>
    <section class="formulario">
        <form action="/admin/create" method="POST">

            <div>
                <label for="title">Title </label>
                <p><input type="text" name="title" id="title"
                    value=<%= locals.old && old.title ? old.title : null %>></p>
                <small><%= locals.errores && errores.title ? errores.title.msg : null %></small>
            </div>

            <div>
                <label for="rating">Rating </label>
                <p><input type="number" name="rating" id="rating"
                    value=<%= locals.old && old.rating ? old.rating : null %>></p>
                <small><%= locals.errores && errores.rating ? errores.rating.msg : null %></small>
            </div>

            <div>
                <label for="awards">Awards </label>
                <p><input type="number" name="awards" id="awards"
                    value=<%= locals.old && old.awards ? old.awards : null %>></p>
                <small><%= locals.errores && errores.awards ? errores.awards.msg : null %></small>
            </div>

            <div>
                <label for="release_date">Release date </label>
                <p><input type="date" name="release_date" id="release_date"
                    value=<%= locals.old && old.release_date ? old.release_date : null %>></p>
                <small><%= locals.errores && errores.release_date ? errores.release_date.msg : null %></small>
            </div>

            <div>
                <label for="length">Length </label>
                <p><input type="number" name="length" id="length"
                    value=<%= locals.old && old.length ? old.length : null %>></p>
                <small><%= locals.errores && errores.length ? errores.length.msg : null %></small> 
            </div>

            <br>
            <p> <button class="botonAgregar" type="submit">Agregar</button>
            
            <a  class="botonVolver" href="/movies">Listado de Películas</a></p>
        </form>
    </section>
    
</body>

</html>